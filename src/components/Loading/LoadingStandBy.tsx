
import React from 'react'
import Loading from './Loading'

function LoadingStandBy() {
  return (
    <Loading label='Stand by' labelSize={16} loadingSize={64} />
  )
}

export default LoadingStandBy